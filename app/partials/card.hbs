<article class="card">
    <img class="card-image" src="{{ card-image }}" width="40" height="40" alt="">
    <h2 class="card-title">{{ card-title }}</h2>
    <div class="card-subtitle">{{ card-subtitle }}</div>
    <q class="card-text"> {{ card-text }} </q>
</article>